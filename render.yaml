# Render.com Blueprint — AI Interview Coach
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: ai-interview-coach
    runtime: python
    region: singapore        # closest to India; change to: ohio, oregon, frankfurt, etc.
    plan: free               # change to "starter" ($7/mo) for always-on
    buildCommand: "./build.sh"
    startCommand: "gunicorn -c deployment/gunicorn.conf.py app:app"
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: "3.12"
      - key: SECRET_KEY
        generateValue: true          # Render auto-generates a secure random value
      - key: DATABASE_URL
        fromDatabase:
          name: ai-interview-coach-db
          property: connectionString
      - key: WTF_CSRF_ENABLED
        value: "True"
      - key: AI_API_KEY
        value: replace-with-your-ai-api-key   # update in Render dashboard

databases:
  - name: ai-interview-coach-db
    databaseName: ai_interview_coach
    user: ai_coach
    plan: free               # 1 GB free PostgreSQL included
